{
  "name": "a",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "docker": "npm run docker -ws --if-present"
  },
  "packageManager": "npm@11.0.0",
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
