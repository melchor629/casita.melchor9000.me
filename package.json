{
  "name": "casita.melchor9000.me",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "change": "changeset",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "docker": "npm run docker --ws --if-present",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:deploy": "turbo run deploy"
  },
  "packageManager": "npm@11.0.0",
  "devDependencies": {
    "@changesets/cli": "^2.0.0",
    "@melchor629/eslint-config": "^0.3.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "prisma": "^7.0.0",
    "react": "npm:@preact/compat@^18",
    "react-dom": "npm:@preact/compat@^18",
    "@types/react": "npm:@preact/compat@^18",
    "turbo": "^2.0.0"
  },
  "#": "https://github.com/prisma/prisma/issues/28752",
  "overrides": {
    "hono": "^4.10.3",
    "valibot": "^1.2.0"
  }
}
