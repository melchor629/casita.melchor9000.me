{
  "name": "a",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "change": "changeset",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "docker": "npm run docker -ws --if-present"
  },
  "packageManager": "npm@11.0.0",
  "devDependencies": {
    "@changesets/cli": "^2.0.0",
    "@melchor629/eslint-config": "^0.2.0",
    "eslint": "^9.0.0",
    "turbo": "^2.0.0"
  }
}
