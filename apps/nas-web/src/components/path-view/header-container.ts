import { styled } from '../core/utils'

const HeaderContainer = styled('div', 'HeaderContainer')({
  base: 'top-0 bg-elevated-0/70 backdrop-blur-lg -mx-3 px-3 pt-navbar pb-2',
})

export default HeaderContainer
