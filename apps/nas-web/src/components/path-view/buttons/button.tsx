import { styled } from 'styled-components'
import { ContextMenuItem } from '../../context-menu'

const Button = styled(ContextMenuItem)`
    display: inline-flex;
    align-items: center;
    column-gap: 0.25rem;
`

export default Button
