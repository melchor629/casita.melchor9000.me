import { styled } from 'styled-components'
import { Settings as SettingsIconBase } from '../icons'

const SettingsIcon = styled(SettingsIconBase)`
  height: 15px;
  margin-bottom: 3px;
`

export default SettingsIcon
